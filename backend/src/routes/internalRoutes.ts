import { Router } from 'express';

const router = Router();

// Internal routes will be added here as needed
// Currently no internal routes are required for the basic functionality

export default router;
